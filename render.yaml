services:
  - type: web
    name: libpulse-app
    runtime: node
    plan: free
    buildCommand: npm run build
    startCommand: npm run start
    envVars:
      - key: MONGO_URI
        sync: false
      - key: PORT
        value: 5000
